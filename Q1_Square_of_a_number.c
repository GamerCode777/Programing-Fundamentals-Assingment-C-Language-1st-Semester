// Question1:   work on a code to find square of a number.

#include <stdio.h>

int main(void)
{
    int number, Square;

    printf("Enter The Number:");
    scanf("%d", & number);
    Square = number*number;
    printf("Square of this number is %d", Square);

return 0;
}